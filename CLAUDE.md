# Claude Code 交互与 Token 效能规范

## 1. 任务前置确认 (Task Pre-check)
- **最小化读取**：在开始新任务前，请先列出你计划读取或修改的文件清单（File Manifest），并简述理由。
- **操作授权**：在我确认清单之前，请勿使用 `cat` 或 `read_file` 批量读取大文件。
- **结构优先**：优先使用 `ls` 或文件树了解项目结构，而非通过 `grep` 扫描全量内容。

## 2. 缓存与上下文监控 (Cache & Context Monitor)
- **主动预警**：当会话长度明显增加，导致缓存读取（Cache Read）显著上升时，请在回复末尾提醒我使用 `/compact` 或 `/clear`。
- **任务分段**：如果一个任务涉及多个独立模块，请引导我分步执行，每步完成后建议清理一次上下文。

## 3. 输出精简 (Lean Output)
- **增量修改**：优先提供代码 Diff 或关键片段（Snippet），除非文件极小，否则避免重新输出整个文件的代码。
- **日志过滤**：在分析日志时，仅提取关键报错堆栈（Error Stack），忽略冗余的调试信息。